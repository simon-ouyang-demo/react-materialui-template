// avoid using semicolons in typescript since it is redundant
module.exports = {
  trailingComma: 'all',
  arrowParens: 'avoid',
  tabWidth: 2,
  semi: false,
  singleQuote: true,
  printWidth: 100,
}
